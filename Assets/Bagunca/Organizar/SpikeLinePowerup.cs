using UnityEngine;

namespace Bagunca.Organizar
{
    public class SpikeLinePowerup : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
